class Products {
  final int? id, price;
  final String? title, suTitle, discribtion, image;

  Products(
      {this.id,
      this.price,
      this.title,
      this.suTitle,
      this.discribtion,
      this.image});
}
